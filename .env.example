# =============================================================================
# Core Configuration
# =============================================================================

# Google Cloud Project ID (required)
PROJECT_ID=your-project-id

# Google Cloud Location (default: us-central1)
LOCATION=us-central1

# Gemini Model ID (default: gemini-live-2.5-flash-native-audio)
MODEL=gemini-live-2.5-flash-native-audio

# Session time limit in seconds (default: 180)
SESSION_TIME_LIMIT=180

# =============================================================================
# Security Configuration
# =============================================================================

# Development Mode - MUST be false in production!
# When true: bypasses ReCAPTCHA validation, uses permissive CORS
DEV_MODE=false

# ReCAPTCHA Enterprise site key (optional - NOT needed if app is only accessible via iframe)
# Only configure this if you need direct public access to the app
# Get this from Google Cloud Console > reCAPTCHA Enterprise
RECAPTCHA_SITE_KEY=

# CORS allowed origins (comma-separated, e.g., "https://example.com,https://www.example.com")
# If not set and DEV_MODE=false, only same-origin requests are allowed
CORS_ORIGINS=https://learn.smartergerman.com,https://courses.smartergerman.com

# Allowed frame ancestors for CSP (comma-separated) - controls which sites can embed this app
# This is the primary access control when the app is only used via iframe embedding
ALLOWED_FRAME_ANCESTORS=https://learn.smartergerman.com,https://courses.smartergerman.com

# Maximum WebSocket message size in bytes (default: 1048576 = 1MB)
MAX_MESSAGE_SIZE=1048576

# =============================================================================
# Rate Limiting (Production)
# =============================================================================

# Redis URL for distributed rate limiting (required for multi-instance deployments)
# Format: redis://[:password@]host:port/db
REDIS_URL=

# Global rate limit (default: 1000 per hour)
GLOBAL_RATE_LIMIT=1000 per hour

# Per-user rate limit (default: 2 per minute)
PER_USER_RATE_LIMIT=2 per minute

# =============================================================================
# Analytics (Optional)
# =============================================================================

# BigQuery dataset for event tracking
BQ_DATASET=

# BigQuery table for event tracking
BQ_TABLE=

# Application name for analytics
DEMO_NAME=immersive-language-learning
